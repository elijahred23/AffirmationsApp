@model List<string>

<div class="permissions" data-user-id="@ViewBag.UserId">
    @foreach (var p in Model)
    {
        <button class="perm-btn btn btn-sm btn-primary me-1"
                data-permission="@p">
            @p
        </button>
    }
</div>